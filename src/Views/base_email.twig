<!doctype html>
<html>

    <head>
        <meta charset="utf-8">
        <title>{{ subject ?? 'Você tem uma nova mensagem' }}</title>
    </head>

    <body>
        <center style="font-family: Arial, Helvetica, sans-serif; font-size: 16px;">
            <h2>Olá, {{ recipient.nome }}!</h2>
            <p>{% block message %}{% endblock %}</p>
            {% if cta is defined %}
            <p>Para continuar, clique no link abaixo:</p>
            <p style="font-size: 20px;">{{ cta|raw }}</p>
            <p style="font-size: 14px;">
                Se o link não funcionar, copie e cole a URL no seu navegador:<br />
                <span style="font-family: monospace;">{{ cta.url }}</span>
            </p>
            {% endif %}
        </center>
    </body>

</html>