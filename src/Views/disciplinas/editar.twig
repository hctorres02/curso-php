{% extends "base.twig" %}

{% block main %}
<section>
    <nav>
        <ul>
            <li>
                <hgroup role="heading">
                    <h3>Editar disciplina</h3>
                </hgroup>
            </li>
        </ul>
    </nav>
    <nav>
        <ul>
            <li>
                <a href="{{ route('disciplinas') }}">&larr; Voltar para listagem</a>
            </li>
        </ul>
        <ul>
            <li>{% include "disciplinas/excluir.twig" %}</li>
        </ul>
    </nav>
</section>
<form action="{{ route('atualizar_disciplina', disciplina) }}" method="post">
    {% include "disciplinas/fieldset.twig" %}
    <input type="hidden" name="_method" value="put">
    <input type="hidden" name="_csrf_token" value="{{ CSRF_TOKEN }}" />
    <input type="submit" value="&#128190; Salvar">
</form>
{% endblock %}