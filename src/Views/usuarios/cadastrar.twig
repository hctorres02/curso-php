{% extends "base.twig" %}

{% block main %}
<section>
    <nav>
        <ul>
            <li>
                <hgroup role="heading">
                    <h3>Cadastro de usuário</h3>
                </hgroup>
            </li>
        </ul>
    </nav>
    <nav>
        <ul>
            <li>
                <a href="{{ route('usuarios') }}">&larr; Voltar para listagem</a>
            </li>
        </ul>
    </nav>
</section>
<form action="{{ route('salvar_usuario') }}" method="post">
    {% include "usuarios/fieldset.twig" %}
    <input type="hidden" name="_csrf_token" value="{{ CSRF_TOKEN }}" />
    <input type="submit" value="&#128190; Cadastrar">
</form>
{% endblock %}